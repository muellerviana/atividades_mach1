/**
 * Na compra de duas unidades de  um mesmo medicamento, o cliente recebe como desconto 20% do valor total
 * Elaborar um programa que leia descricao e o preço de um medicamento. Informe o valor do produto na promoção.
 * Exemplo:
 * Medicamento: Dipirona
 * Preço R$: 10,00
 * Saída Esperada => Promoção de Dipirona: Leve dois por R$ 18,00
 */


 const prompt = require('prompt-sync')();


//  Pergunta qual o remédio
  //  fazer um prompt e guardar na variável
//  qual a quantidade 
  // verificar se e mais de um
    //  criar logica para add o desconto
//  quer algo mais? y/n
// se sim = guarda os dados
// se não = calcula e retorna valor total